{
  "name": "@easyagent/backend",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@easyagent/lib": "workspace:*",
    "@easyagent/pyth-fetcher": "workspace:*",
    "@hono/node-server": "^1.4.0",
    "config": "^3.3.12",
    "dotenv": "^16.4.7",
    "hono": "^4.6.20",
    "toml": "^3.0.0",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/config": "^3.3.5",
    "@types/node": "^20.11.17",
    "ts-node-dev": "^2.0.0",
    "typescript": "~5.7.2"
  }
}